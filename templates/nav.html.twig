<nav class="row navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav text-uppercase font-weight-bold">
        <li class="nav-item">
            <a class="nav-link" href="#">Accueil<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Articles</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Ventes</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Contactez-nous</a>
        </li>
        </ul>
    </div>
    <div class="collapse navbar-collapse" id="navbarNav">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="./index.php/profile" class="btn border border-secondary">{{ app.user }}</a>
        <button type="button" class="btn border border-secondary"><a class="link" href="{{ path('fos_user_security_logout') }}">Deconnexion</a></button>
        {% else %}
        <button type="button" class="btn border border-secondary"><a class="link" href="{{ path('fos_user_security_login') }}">Connexion</a></button>
        {% endif %}
        <button type="button" class="btn border border-secondary"><a class="link" href="{{ path('fos_user_registration_register') }}">Inscription</a></button>
    </div>
</nav>